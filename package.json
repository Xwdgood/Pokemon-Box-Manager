{
  "name": "pokemon-box-manager",
  "version": "1.0.0",
  "description": "A Pokemon collection management system built with the MERN stack",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 